<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        POST_NOTIFICATIONS — required at runtime on API 33+. We also request it from
        MainActivity before posting any Live Update.
    -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!--
        POST_PROMOTED_NOTIFICATIONS — normal-protection permission required by Android 16
        (API 36) for the system to upgrade an ongoing notification to a status-bar chip
        + top-of-shade Live Update. No runtime request needed; manifest declaration alone
        is sufficient. Without this, the notification posts but renders as a normal one.
    -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />

    <application
        android:name=".PizzaApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">
        <activity
            android:exported="true"
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
